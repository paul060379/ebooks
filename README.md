# Ollie Ebook Shelf

這個 repo 現在是一本書櫃網站：

- `spider/`
- `hulk/`

網站入口：

- `/` 是書櫃首頁
- `/spider/` 是蜘蛛英雄那本
- `/hulk/` 是浩克刷牙那本

相容舊路徑：

- `/ebook/` 會轉到 `/`
- `/ebook/spider/` 會轉到 `/spider/`
- `/ebook/hulk/` 會轉到 `/hulk/`
