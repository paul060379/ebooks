# Ollie Ebook Shelf

目前主路徑：

- `/bookshelf/`：書櫃首頁
- `/books/spider/`：蜘蛛英雄那本
- `/books/hulk/`：浩克刷牙那本

英文命名：

- `bookshelf` = 書櫃
- `books` = 書本集合

相容舊路徑：

- `/` 會轉到 `/bookshelf/`
- `/spider/` 會轉到 `/books/spider/`
- `/hulk/` 會轉到 `/books/hulk/`
- `/ebook/` 會轉到 `/bookshelf/`
- `/ebook/spider/` 會轉到 `/books/spider/`
- `/ebook/hulk/` 會轉到 `/books/hulk/`
